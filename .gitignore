# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg
*.egg-info/
dist/
build/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.whl
MANIFEST

# Virtual environments
.env
.venv
env/
venv/
ENV/

# Environment variables (NEVER commit these)
.env.local
.env.production
.env.*.local

# Database files
*.db
*.sqlite
*.sqlite3

# Logs
*.log
logs/

# Playwright browsers (installed at build time)
.playwright/

# Large files
*.zip
*.tar.gz

# OS files
.DS_Store
.DS_Store?
._*
.Spotlight-V7
.Trashes
ehthumbs.db
Thumbs.db

# IDEs
.idea/
.vscode/
*.swp
*.swo

# Compiled assets / uploads
static/uploads/
outputs/
support_requests.jsonl

# Replit-specific (no longer needed)
.replit
replit.md

# Test databases
test_*.db

# Local Flask-Migrate scratch (used to autogenerate migrations against an
# isolated sqlite — never points at prod). Safe to delete anytime.
.flask_migrate_workdir/

# Local reference material exported from Replit (logo, pasted prompts, screenshots)
attached_assets/

# Loose secret note (Flask session key) — never commit
DOMINATE-MArketing session secret.txt
*session secret*.txt

# Railway API token (local only — NEVER commit)
railway_token.txt
*_token.txt
